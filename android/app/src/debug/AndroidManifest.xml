<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug-only overlay. The manifest merger folds this into the debug variant's
  manifest, attaching the debug network security config (which permits cleartext
  to LAN dev servers). Release builds never see it. See res/xml/network_security_config.xml.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:networkSecurityConfig="@xml/network_security_config" />

</manifest>
